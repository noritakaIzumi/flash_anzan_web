import { type CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'com.fairyselect.aurora_flash',
    appName: 'aurora-flash',
    webDir: 'dist',
    server: {
        androidScheme: 'https',
    },
}

export default config
