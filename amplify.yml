version: 1
frontend:
  phases:
    build:
      commands:
        - npm install
        - npm run build
    postBuild:
      commands:
        - test -d dist
        - find dist
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
